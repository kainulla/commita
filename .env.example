# GitHub OAuth App credentials (required for private repo support)
# Create at: https://github.com/settings/developers → OAuth Apps → New
# Set callback URL to: http://localhost:3000/auth/callback
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

PORT=3000

CACHE_TTL=3600

# Upstash Redis (required in production, auto-set when you connect Upstash for Redis
# via Vercel Marketplace → Storage → Upstash for Redis)
# For local dev: copy from Vercel project Settings → Environment Variables
UPSTASH_REDIS_REST_URL=
UPSTASH_REDIS_REST_TOKEN=
